# Rust
/target
target/
# Artefacts de build intermédiaires (ex. en-têtes pour l'XCFramework)
/build
**/*.rs.bk
Cargo.lock.orig

# Bindings générés au build — source unique de vérité = champinium-core.
# JAMAIS commités (régénérés par le justfile).
/bindings/
**/*.xcframework

# Swift / Xcode
apps/macos/.build/
apps/macos/.swiftpm/
apps/macos/DerivedData/
*.xcuserstate
# Artefacts générés au build (bindings + framework), jamais commités.
apps/macos/Frameworks/
apps/macos/Sources/ChampiniumCore/

# C# / WinUI / .NET
apps/windows/**/bin/
apps/windows/**/obj/
apps/windows/**/*.user

# OS
.DS_Store
Thumbs.db
