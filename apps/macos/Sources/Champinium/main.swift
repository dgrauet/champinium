// Champinium — front macOS natif SwiftUI (SQUELETTE).
//
// Présentation UNIQUEMENT. La logique (P2P, catalogue, modération, lecture)
// vit dans champinium-core (Rust) et sera appelée via le module Swift généré
// `ChampiniumCore` (UniFFI). Lecture média cible : AVPlayer / AVFoundation.
//
// Quand les bindings seront générés (`just gen-swift`), remplacer ce placeholder
// par une App SwiftUI : `import ChampiniumCore` puis `coreVersion()`.

// TODO Phase 3 (MVP) : App SwiftUI — catalogue + lecture AVPlayer.
print("Champinium macOS (squelette SwiftUI) — bindings UniFFI à générer via `just gen-swift`.")
